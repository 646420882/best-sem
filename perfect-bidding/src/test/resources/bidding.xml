<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
	    http://www.springframework.org/schema/context/spring-context.xsd"
       xmlns:context="http://www.springframework.org/schema/context"
       default-autowire="byName">

    <context:property-placeholder
            location="classpath:mongodb.properties,classpath:elasticsearch.properties,classpath:redis.properties"/>

    <context:annotation-config/>
    <context:component-scan base-package="com.perfect"/>

    <!-- 任务启动类 -->
    <bean id="jobBoostrap" class="com.perfect.bidding.core.JobBoostrap" init-method="start"/>

    <!--<bean name="requestHelper" class="com.perfect.api.baidu.RequestHelper">-->
    <!--<property name="semaphoreValue" value="1"/>-->
    <!--</bean>-->

    <!-- 单账户独立进程 -->
    <bean name="accountThreadTaskExecutors" class="com.perfect.bidding.core.AccountThreadTaskExecutors">
        <property name="threadNamePrefix" value="account-thread-"/>
        <property name="corePoolSize" value="8"/>
    </bean>

    <!-- 心跳任务 -->
    <bean name="heartbeatTask" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject">
            <bean class="com.perfect.bidding.heartbeat.HeartBeatJob"/>
        </property>
        <property name="targetMethod">
            <value>start</value>
        </property>
    </bean>

    <bean name="cronTrigger" class="org.springframework.scheduling.quartz.CronTriggerBean">
        <property name="jobDetail" ref="heartbeatTask"/>
        <property name="cronExpression" value="0 0/1 * * * ?"/>
    </bean>

    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="autoStartup" value="true"/>
        <property name="triggers">
            <list>
                <ref bean="cronTrigger"/>
            </list>
        </property>
    </bean>

    <!-- ============================================ -->
    <import resource="spring-es.xml"/>
    <import resource="spring-mongodb.xml"/>

    <!-- ============================================= -->

</beans>